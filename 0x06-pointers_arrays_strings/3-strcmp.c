#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strcmp - compare the string
 * @a: dest
 * @b: src
 * Return: Return the value of comparaison
 */
int _strcmp(char *a, char *b)
{
	return (strcmp(a, b));
}
